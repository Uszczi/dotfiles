- defaults:
    link:
        create: true
        relink: true

- clean: ['~', '~/.config']

- link:
    ~/.alacritty.yml: alacritty.yml
    ~/.config/nvim: nvim
    ~/.bashrc: bashrc
    ~/.tmux.conf: tmux.conf

- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.bashrc: bashrc
    ~/.config/fish: fish
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.config/qtile: qtile
    ~/.config/rofi/config.rasi: rofi-config.rasi
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.ocamlinit: ocamlinit
    ~/.Xmodmap: Xmodmap
    # ~/.alacritty.yml: alacritty.yml
    # ~/.config/nnn: nnn
    # ~/.config/pylintrc: pylintrc
    # ~/.xinitrc: xinitrc
    # ~/.doom.d: doom
    # ~/wallpapers2: wallpapers

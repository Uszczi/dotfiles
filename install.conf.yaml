- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.bashrc: bashrc
    ~/.config/fish: fish
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.config/qtile: qtile
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.ocamlinit: ocamlinit
    # ~/.alacritty.yml: alacritty.yml
    # ~/.config/nnn: nnn
    # ~/.config/pylintrc: pylintrc
    # ~/.xinitrc: xinitrc
    # ~/.doom.d: doom
    # ~/wallpapers2: wallpapers
